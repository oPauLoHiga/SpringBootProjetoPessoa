# Copie este arquivo como application-local.properties e preencha apenas no seu computador.
# O arquivo real é ignorado pelo Git e não entra no pacote da aplicação.
spring.datasource.url=jdbc:mysql://localhost:3306/cadastro_pessoas_db?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sua_senha_do_mysql

# Preencha quando for criar o primeiro administrador.
app.admin.email=
app.admin.password=
